machine:
  python:
    version: 3.5.1
  services:
    - rabbitmq-server

dependencies:
  pre:
    - pip install six
  override:
    - pip install -r requirements.freeze.txt
  post:
    - npm install -g less

test:
  override:
    - ./runtests.sh
  post:
    - coveralls
